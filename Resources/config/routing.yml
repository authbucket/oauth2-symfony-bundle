authbucket_oauth2_authorize:
    pattern: /oauth2/authorize
    defaults: { _controller: AuthBucketOAuth2Bundle:Authorize:authorize }
    methods: [GET]

authbucket_oauth2_token:
    pattern: /oauth2/token
    defaults: { _controller: AuthBucketOAuth2Bundle:Token:token }
    methods: [POST]

authbucket_oauth2_resource_username:
    pattern: /oauth2/resource/username
    defaults: { _controller: AuthBucketOAuth2Bundle:Resource:username }
