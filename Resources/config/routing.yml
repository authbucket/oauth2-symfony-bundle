oauth2_authorize:
    pattern: /authorize
    defaults: { _controller: Oauth2Bundle:Authorize:authorize }
    methods: [GET]

oauth2_token:
    pattern: /token
    defaults: { _controller: Oauth2Bundle:Token:token }
    methods: [POST]

oauth2_resource_username:
    pattern: /resource/username
    defaults: { _controller: Oauth2Bundle:Resource:username }
