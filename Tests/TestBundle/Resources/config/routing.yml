index:
    pattern:    /
    defaults:   { _controller:TestBundle:Default:index }

admin_refresh_database:
    pattern:    /admin/refresh_database

demo_index:
    pattern:    /demo
    defaults:   { _controller:TestBundle:Demo:demoIndex }

client_index:
    pattern:    /client
    defaults:   { _controller:TestBundle:Client:clientIndex }

resource_index:
    pattern:    /resource
    defaults:   { _controller:TestBundle:Resource:resourceIndex }

oauth2_index:
    pattern:    /oauth2
    defaults:   { _controller:TestBundle:OAuth2:oauth2Index }

oauth2_login:
    pattern:    /oauth2/login
    defaults:   { _controller:TestBundle:OAuth2:oauth2Login }

oauth2_authorize_form_login_check:
    pattern:    /oauth2/authorize/form/login_check
